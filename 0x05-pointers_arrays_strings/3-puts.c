#include "main.h"
/**
 * _puts - the function prints a string follwed by a new line
 *
 * @str: prints a string
 */
void _puts(char *str)
{
	int 1;

	for (i = 0, str[i] != '\0'; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
