#include "main.h"
/**
 * multip - the function checks for a digit 0 through
 * @a: a - is a variable
 * @b: b - is a variable
 * Return: Always 0.
 */
int multip(int a, int b)
{
	int multip;

	multip = a * b;
	return (multip);
}
